[package]
name = "variegated-tlc59108"
version = "0.1.0"
edition = "2021"
description = "Async driver for the TI TLC59108 8-bit I2C LED driver"
repository = "https://github.com/variegated-coffee/variegated-rs"
license = "MIT OR Apache-2.0"
keywords = ["embedded", "async", "tlc59108", "led", "i2c"]
categories = ["embedded", "hardware-support", "no-std"]

[lib]
test = false
bench = false

[features]
default = []
defmt = ["dep:defmt"]

[dependencies]
embedded-hal-async = "1.0"
defmt = { version = "0.3", optional = true }

[dev-dependencies]
embassy-executor = { version = "0.7.0", features = ["arch-cortex-m", "executor-thread", "defmt"] }
embassy-time = { version = "0.4.0", features = ["defmt"] }
embassy-rp = { version = "0.6.0", features = ["defmt", "time-driver", "rp235xb"] }
embassy-embedded-hal = { version = "0.3.0", features = ["defmt"] }
cortex-m-rt = "0.7.0"
defmt = "0.3"
defmt-rtt = "0.4"
panic-probe = { version = "0.3", features = ["print-defmt"] }